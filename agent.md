# Contexto del agente

Las instrucciones operativas canónicas están en [AGENTS.md](AGENTS.md).

Antes de modificar código, confirma que el workspace es `MT5_Autotester_agent_manager`, usa `codebase-memory-mcp` y consulta `ai_context/` para decisiones persistentes.
